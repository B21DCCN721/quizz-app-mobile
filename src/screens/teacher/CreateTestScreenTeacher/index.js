import { Text } from "react-native";
import DefaultLayout from "../../../layouts/DefaultLayout";

function CreateTestScreenTeacher() {
    return (  
        <DefaultLayout>
            <Text>Màn hình tạo mới bài thi</Text>
        </DefaultLayout>
    );
}

export default CreateTestScreenTeacher;